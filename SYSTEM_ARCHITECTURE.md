# PicoClaw System Architecture

**Version**: 1.0.0
**Date**: February 15, 2026
**Status**: Production Ready with Azure OpenAI Support

---

## 📋 Table of Contents

1. [System Overview](#system-overview)
2. [High-Level Architecture](#high-level-architecture)
3. [Component Architecture](#component-architecture)
4. [Data Flow Architecture](#data-flow-architecture)
5. [Deployment Architecture](#deployment-architecture)
6. [Azure Integration](#azure-integration)
7. [Technology Stack](#technology-stack)
8. [Security Architecture](#security-architecture)

---

## 🎯 System Overview

PicoClaw is an **ultra-lightweight personal AI agent** with multi-channel support, extensible skills, and enterprise-grade LLM provider integration including Azure OpenAI with Managed Identity.

### Key Capabilities

- 🤖 **Multi-Provider AI**: Claude (Anthropic), OpenAI, Azure OpenAI, Gemini, DeepSeek, and more
- 🔌 **Multi-Channel**: Discord, Telegram, Slack, LINE, WhatsApp, WeChat, QQ, DingTalk, Feishu
- 🛠️ **Extensible Tools**: Filesystem, shell, web, cron, I2C, SPI, voice
- 📦 **Skills System**: Plugin architecture for custom capabilities
- 🔐 **Enterprise Auth**: OAuth, API Keys, Azure Managed Identity, Keychain integration
- 💾 **State Management**: Session history, context memory, persistent storage
- 🚀 **Deployment Flexibility**: Local, Docker, Azure (App Service, VM, Container Instance)

---

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │ Discord  │ │ Telegram │ │  Slack   │ │   LINE   │ │ WhatsApp │  │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘  │
│       │            │            │            │            │         │
│  ┌────┴────────────┴────────────┴────────────┴────────────┴──────┐  │
│  │              CLI / Interactive Terminal                        │  │
│  └─────────────────────────────┬─────────────────────────────────┘  │
└────────────────────────────────┼───────────────────────────────────┘
                                 │
┌────────────────────────────────┼───────────────────────────────────┐
│                        GATEWAY LAYER                                │
│  ┌──────────────────────────────▼───────────────────────────────┐  │
│  │                      Message Bus                              │  │
│  │  • Event routing • Channel abstraction • Message queuing      │  │
│  └──────────────────────────────┬───────────────────────────────┘  │
└────────────────────────────────┼───────────────────────────────────┘
                                 │
┌────────────────────────────────┼───────────────────────────────────┐
│                         AGENT CORE                                  │
│  ┌──────────────────────────────▼───────────────────────────────┐  │
│  │                      Agent Loop                               │  │
│  │  ┌─────────────┐  ┌──────────────┐  ┌────────────────┐      │  │
│  │  │  Context    │  │   Session    │  │  Tool Loop     │      │  │
│  │  │  Builder    │  │   Manager    │  │  Executor      │      │  │
│  │  └─────────────┘  └──────────────┘  └────────────────┘      │  │
│  └──────────┬───────────────┬────────────────┬──────────────────┘  │
└─────────────┼───────────────┼────────────────┼──────────────────────┘
              │               │                │
    ┌─────────▼────┐  ┌───────▼─────┐  ┌──────▼───────┐
    │              │  │             │  │              │
┌───▼──────────────▼──▼─────────────▼──▼──────────────▼────────────┐
│                      SERVICE LAYER                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │  Providers   │  │    Skills    │  │    Tools     │           │
│  │  • Claude    │  │  • Custom    │  │  • Filesystem│           │
│  │  • OpenAI    │  │  • Built-in  │  │  • Shell     │           │
│  │  • Azure AI  │  │  • Community │  │  • Web       │           │
│  │  • Gemini    │  │  • Local     │  │  • Cron      │           │
│  │  • DeepSeek  │  │              │  │  • Voice     │           │
│  │  • Codex     │  │              │  │  • I2C/SPI   │           │
│  └──────────────┘  └──────────────┘  └──────────────┘           │
└───────────────────────────────────────────────────────────────────┘
              │               │                │
┌─────────────▼───────────────▼────────────────▼────────────────────┐
│                     INFRASTRUCTURE LAYER                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │    State     │  │    Auth      │  │   Config     │           │
│  │  • Sessions  │  │  • OAuth     │  │  • .env      │           │
│  │  • Memory    │  │  • API Keys  │  │  • JSON      │           │
│  │  • History   │  │  • Managed   │  │  • Runtime   │           │
│  │  • Cache     │  │    Identity  │  │              │           │
│  └──────────────┘  └──────────────┘  └──────────────┘           │
│                                                                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │   Logger     │  │  Heartbeat   │  │   Migrate    │           │
│  │  • Levels    │  │  • Health    │  │  • Versioning│           │
│  │  • Rotation  │  │  • Monitor   │  │  • DB Schema │           │
│  └──────────────┘  └──────────────┘  └──────────────┘           │
└───────────────────────────────────────────────────────────────────┘
```

---

## 🔧 Component Architecture

### 1. Client Layer

**Purpose**: Multi-channel input/output interfaces

```
┌─────────────────────────────────────────────────────────────┐
│                      Client Channels                         │
├──────────────┬──────────────┬──────────────┬────────────────┤
│  Messaging   │  Social      │  Enterprise  │  Development   │
├──────────────┼──────────────┼──────────────┼────────────────┤
│ • Discord    │ • WeChat     │ • Slack      │ • CLI          │
│ • Telegram   │ • QQ         │ • DingTalk   │ • Interactive  │
│ • LINE       │ • WhatsApp   │ • Feishu     │ • Terminal     │
└──────────────┴──────────────┴──────────────┴────────────────┘
```

**Key Files**:
- `pkg/channels/discord.go` - Discord integration
- `pkg/channels/telegram.go` - Telegram bot
- `pkg/channels/slack.go` - Slack app
- `pkg/channels/line.go` - LINE messenger
- `pkg/channels/whatsapp.go` - WhatsApp integration
- `cmd/picoclaw/main.go` - CLI interface

**Capabilities**:
- Message reception from multiple platforms
- Response formatting per platform
- Media handling (images, files, voice)
- Platform-specific features (buttons, cards, reactions)

---

### 2. Gateway Layer (Message Bus)

**Purpose**: Event routing and channel abstraction

```
┌─────────────────────────────────────────────────────────────┐
│                      Message Bus                             │
│                                                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              Event Queue (FIFO)                       │   │
│  │  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐     │   │
│  │  │ Msg 1  │→ │ Msg 2  │→ │ Msg 3  │→ │ Msg N  │     │   │
│  │  └────────┘  └────────┘  └────────┘  └────────┘     │   │
│  └──────────────────────────────────────────────────────┘   │
│                           ↓                                   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │           Channel Routing Logic                       │   │
│  │  • Source identification                              │   │
│  │  • Target channel mapping                             │   │
│  │  • Message transformation                             │   │
│  │  • Error handling                                     │   │
│  └──────────────────────────────────────────────────────┘   │
│                           ↓                                   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              Agent Loop Dispatcher                    │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**Key Files**:
- `pkg/bus/bus.go` - Message bus implementation

**Message Structure**:
```go
type Message struct {
    SessionKey string        // Session identifier
    Channel    string        // Source channel (discord, telegram, etc.)
    ChatID     string        // Chat/room identifier
    SenderID   string        // User identifier
    Content    string        // Message content
    Timestamp  time.Time     // Message timestamp
    Metadata   map[string]interface{}
}
```

---

### 3. Agent Core

**Purpose**: Orchestrate AI interactions and tool execution

```
┌─────────────────────────────────────────────────────────────┐
│                        Agent Loop                            │
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  1. Message Reception                                │    │
│  │     ↓                                                 │    │
│  │  2. Context Building                                  │    │
│  │     • Load session history                            │    │
│  │     • Add system instructions                         │    │
│  │     • Include relevant tools                          │    │
│  │     ↓                                                 │    │
│  │  3. LLM Provider Call                                 │    │
│  │     • Select provider (Claude, OpenAI, Azure, etc.)   │    │
│  │     • Execute request                                 │    │
│  │     ↓                                                 │    │
│  │  4. Response Processing                               │    │
│  │     ┌─────────────────┬─────────────────┐            │    │
│  │     │  Direct Answer  │   Tool Calls    │            │    │
│  │     └────────┬────────┴────────┬────────┘            │    │
│  │              │                 │                      │    │
│  │              ↓                 ↓                      │    │
│  │     ┌────────────┐    ┌──────────────┐              │    │
│  │     │   Send     │    │  Execute     │              │    │
│  │     │  Response  │    │   Tools      │              │    │
│  │     └────────────┘    └──────┬───────┘              │    │
│  │                              │                       │    │
│  │                              ↓                       │    │
│  │                    ┌──────────────────┐             │    │
│  │                    │  Loop back to 3  │             │    │
│  │                    │  (max iterations)│             │    │
│  │                    └──────────────────┘             │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

**Key Files**:
- `pkg/agent/loop.go` - Main agent loop
- `pkg/agent/context.go` - Context builder
- `pkg/agent/memory.go` - Memory management
- `pkg/session/session.go` - Session management

**Agent Loop Components**:

#### 3.1 Context Builder
```go
type ContextBuilder struct {
    // Builds conversation context with:
    // - System instructions
    // - Session history
    // - Tool definitions
    // - User message
}
```

#### 3.2 Session Manager
```go
type SessionManager struct {
    // Manages:
    // - Conversation history per session
    // - Context window management
    // - Memory compression/summarization
}
```

#### 3.3 Tool Loop Executor
```go
type ToolLoop struct {
    // Executes tool calls:
    // - Parallel tool execution
    // - Error handling
    // - Result aggregation
    // - Iteration limits
}
```

---

### 4. Providers Layer

**Purpose**: LLM provider abstraction and integration

```
┌─────────────────────────────────────────────────────────────┐
│                     LLM Providers                            │
├──────────────┬──────────────┬──────────────┬────────────────┤
│  Anthropic   │   OpenAI     │  Azure AI    │    Others      │
├──────────────┼──────────────┼──────────────┼────────────────┤
│ • Claude     │ • GPT-4o     │ • Azure      │ • Gemini       │
│   Sonnet     │ • GPT-4      │   OpenAI     │ • DeepSeek     │
│ • Claude     │ • Codex      │ • Managed    │ • Groq         │
│   Opus       │ • o1/o3      │   Identity   │ • OpenRouter   │
│ • Claude     │              │ • Default    │ • Zhipu/GLM    │
│   Haiku      │              │   Azure      │ • Moonshot     │
│              │              │   Credential │ • Local LLMs   │
│ Auth:        │ Auth:        │ Auth:        │ Auth:          │
│ • API Key    │ • API Key    │ • Bearer     │ • API Key      │
│ • Keychain   │ • OAuth      │   Token      │ • Custom       │
│ • OAuth      │              │ • MI Client  │                │
│              │              │   ID         │                │
└──────────────┴──────────────┴──────────────┴────────────────┘
```

**Key Files**:
- `pkg/providers/types.go` - Provider interface
- `pkg/providers/claude_provider.go` - Anthropic Claude
- `pkg/providers/codex_provider.go` - OpenAI & **Azure OpenAI**
- `pkg/providers/github_copilot_provider.go` - GitHub Copilot
- `pkg/providers/http_provider.go` - HTTP-based providers

**Provider Interface**:
```go
type LLMProvider interface {
    Chat(ctx context.Context,
         messages []Message,
         tools []ToolDefinition,
         model string,
         options map[string]interface{}) (*LLMResponse, error)

    GetDefaultModel() string
}
```

**Azure OpenAI Provider Flow**:
```
┌──────────────────────────────────────────────────────────┐
│         Azure OpenAI Provider (Codex)                     │
│                                                            │
│  1. Configuration Detection                                │
│     ├─ AZURE_OPENAI_ENDPOINT set?                         │
│     ├─ AZURE_OPENAI_DEPLOYMENT set?                       │
│     └─ AZURE_OPENAI_API_VERSION set?                      │
│                    ↓ YES                                   │
│  2. Authentication                                         │
│     ├─ MANAGED_IDENTITY_CLIENT_ID set?                    │
│     │  ├─ YES → UserAssignedManagedIdentity              │
│     │  └─ NO  → DefaultAzureCredential                    │
│     │          (Azure CLI for local, MI for Azure)        │
│     └─ Get Bearer Token                                    │
│                    ↓                                       │
│  3. API Call                                               │
│     ├─ Endpoint: /openai/deployments/{deployment}         │
│     ├─          /chat/completions                         │
│     ├─ Query: ?api-version={version}                      │
│     ├─ Auth: Bearer {token}                               │
│     └─ Body: Chat Completion params                       │
│                    ↓                                       │
│  4. Response Parsing                                       │
│     └─ Convert to LLMResponse                             │
└──────────────────────────────────────────────────────────┘
```

---

### 5. Skills Layer

**Purpose**: Extensible plugin system for custom capabilities

```
┌─────────────────────────────────────────────────────────────┐
│                      Skills System                           │
│                                                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              Skill Loader                             │   │
│  │  • Discover skills in ~/.picoclaw/skills/            │   │
│  │  • Load built-in skills                              │   │
│  │  • Validate skill manifests                          │   │
│  │  • Initialize skill instances                        │   │
│  └──────────────────┬───────────────────────────────────┘   │
│                     │                                        │
│  ┌──────────────────▼───────────────────────────────────┐   │
│  │              Skill Registry                           │   │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐     │   │
│  │  │  Built-in  │  │   Custom   │  │ Community  │     │   │
│  │  │  Skills    │  │   Skills   │  │  Skills    │     │   │
│  │  └────────────┘  └────────────┘  └────────────┘     │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │           Skill Execution Context                     │   │
│  │  • Access to tools                                    │   │
│  │  • Access to state                                    │   │
│  │  • Access to configuration                            │   │
│  │  • Isolated execution environment                     │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**Key Files**:
- `pkg/skills/loader.go` - Skill loading
- `pkg/skills/installer.go` - Skill installation

**Skill Structure**:
```
~/.picoclaw/skills/
├── skill-name/
│   ├── manifest.json      # Skill metadata
│   ├── main.py/main.js    # Skill code
│   └── README.md          # Documentation
```

---

### 6. Tools Layer

**Purpose**: Built-in capabilities for agent interactions

```
┌─────────────────────────────────────────────────────────────┐
│                        Tools Registry                        │
├──────────────┬──────────────┬──────────────┬────────────────┤
│  Filesystem  │   Execution  │  External    │   Hardware     │
├──────────────┼──────────────┼──────────────┼────────────────┤
│ • Read file  │ • Shell cmd  │ • Web fetch  │ • I2C read     │
│ • Write file │ • Spawn proc │ • Web search │ • I2C write    │
│ • List dir   │ • Cron job   │ • API calls  │ • SPI xfer     │
│ • Edit file  │ • Subagent   │              │ • GPIO         │
└──────────────┴──────────────┴──────────────┴────────────────┘
```

**Key Files**:
- `pkg/tools/registry.go` - Tool registration
- `pkg/tools/filesystem.go` - File operations
- `pkg/tools/shell.go` - Shell command execution
- `pkg/tools/web.go` - Web operations
- `pkg/tools/cron.go` - Scheduled tasks
- `pkg/tools/subagent.go` - Nested agent calls
- `pkg/tools/i2c.go` - I2C communication
- `pkg/tools/spi.go` - SPI communication
- `pkg/tools/message.go` - Messaging tools

**Tool Definition**:
```go
type Tool struct {
    Name        string
    Description string
    Parameters  map[string]interface{}
    Execute     func(args map[string]interface{}) (string, error)
}
```

---

### 7. Infrastructure Layer

#### 7.1 State Management

```
┌─────────────────────────────────────────────────────────────┐
│                      State Manager                           │
│                                                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              Session State                            │   │
│  │  • Conversation history                               │   │
│  │  • Context window                                     │   │
│  │  • Last activity timestamp                            │   │
│  │  • User preferences                                   │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              Global State                             │   │
│  │  • Agent configuration                                │   │
│  │  • Active sessions                                    │   │
│  │  • Cron jobs                                          │   │
│  │  • System metrics                                     │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              Persistence                              │   │
│  │  • SQLite database                                    │   │
│  │  • File-based storage                                 │   │
│  │  • Memory cache                                       │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**Key Files**:
- `pkg/state/state.go` - State manager
- `pkg/session/session.go` - Session storage

#### 7.2 Authentication

```
┌─────────────────────────────────────────────────────────────┐
│                  Authentication System                       │
├──────────────┬──────────────┬──────────────┬────────────────┤
│  Provider    │   Method     │   Storage    │   Features     │
├──────────────┼──────────────┼──────────────┼────────────────┤
│ Anthropic    │ • API Key    │ • Keychain   │ • Auto-detect  │
│              │ • Keychain   │ • File       │ • Multi-source │
│              │ • OAuth      │ • Env var    │ • Fallback     │
├──────────────┼──────────────┼──────────────┼────────────────┤
│ OpenAI       │ • API Key    │ • File       │ • OAuth flow   │
│              │ • OAuth      │ • Env var    │ • Refresh      │
├──────────────┼──────────────┼──────────────┼────────────────┤
│ Azure OpenAI │ • Bearer     │ • Azure AD   │ • Managed ID   │
│              │   Token      │ • Azure CLI  │ • Default      │
│              │ • Managed    │              │   Credential   │
│              │   Identity   │              │ • Local test   │
└──────────────┴──────────────┴──────────────┴────────────────┘
```

**Key Files**:
- `pkg/auth/auth.go` - Authentication manager
- `pkg/auth/oauth.go` - OAuth flows

---

## 📊 Data Flow Architecture

### Request Flow

```
┌────────────┐
│   User     │
│  Message   │
└─────┬──────┘
      │
      ▼
┌────────────────────────────────────────────┐
│          1. Channel Reception              │
│  Discord/Telegram/Slack/CLI receives msg   │
└─────┬──────────────────────────────────────┘
      │
      ▼
┌────────────────────────────────────────────┐
│          2. Message Bus Routing            │
│  • Parse channel                           │
│  • Create Message struct                   │
│  • Publish to agent loop                   │
└─────┬──────────────────────────────────────┘
      │
      ▼
┌────────────────────────────────────────────┐
│          3. Agent Loop Processing          │
│  ┌──────────────────────────────────────┐  │
│  │ 3.1 Context Building                 │  │
│  │  • Load session history              │  │
│  │  • Add system instructions           │  │
│  │  • Prepare tool definitions          │  │
│  └──────────────┬───────────────────────┘  │
│                 ▼                           │
│  ┌──────────────────────────────────────┐  │
│  │ 3.2 LLM Provider Selection           │  │
│  │  • Determine provider (config)       │  │
│  │  • Select model                      │  │
│  │  • Prepare request                   │  │
│  └──────────────┬───────────────────────┘  │
│                 ▼                           │
│  ┌──────────────────────────────────────┐  │
│  │ 3.3 Provider API Call                │  │
│  │  • Authenticate                      │  │
│  │  • Send request                      │  │
│  │  • Receive response                  │  │
│  └──────────────┬───────────────────────┘  │
│                 ▼                           │
│  ┌──────────────────────────────────────┐  │
│  │ 3.4 Response Processing              │  │
│  │  ┌────────────────┬────────────────┐ │  │
│  │  │ Direct Answer  │  Tool Calls    │ │  │
│  │  └────────┬───────┴────────┬───────┘ │  │
│  └───────────┼────────────────┼─────────┘  │
└──────────────┼────────────────┼────────────┘
               │                │
         ┌─────▼────┐    ┌──────▼───────┐
         │  Return  │    │   Execute    │
         │ Response │    │    Tools     │
         └─────┬────┘    └──────┬───────┘
               │                │
               │         ┌──────▼───────────┐
               │         │  Tool Execution  │
               │         │  • Parallel exec │
               │         │  • Error handle  │
               │         │  • Result agg    │
               │         └──────┬───────────┘
               │                │
               │         ┌──────▼───────────┐
               │         │  Loop back to    │
               │         │  Agent (3.3)     │
               │         └──────────────────┘
               │
      ▼
┌────────────────────────────────────────────┐
│          4. Response Delivery              │
│  • Format for channel                      │
│  • Send via message bus                    │
│  • Handle attachments/media                │
└─────┬──────────────────────────────────────┘
      │
      ▼
┌────────────┐
│   User     │
│  Receives  │
│  Response  │
└────────────┘
```

### Azure OpenAI Authentication Flow

```
┌────────────────────────────────────────────────────────────┐
│              Local Development Flow                         │
│                                                              │
│  1. User runs: picoclaw agent -m "question"                 │
│                      ↓                                       │
│  2. LoadAzureConfigFromEnv()                                │
│     • Detect AZURE_OPENAI_* env vars                        │
│     • MANAGED_IDENTITY_CLIENT_ID is empty                   │
│                      ↓                                       │
│  3. NewCodexProviderAuto()                                  │
│     • Azure config detected                                 │
│     • Creates Azure provider                                │
│                      ↓                                       │
│  4. createAzureManagedIdentityTokenSource()                 │
│     • config.ManagedIdentityID == ""                        │
│     • Uses DefaultAzureCredential()                         │
│                      ↓                                       │
│  5. DefaultAzureCredential tries:                           │
│     ├─ Environment variables (service principal) ❌          │
│     ├─ Managed Identity (not in Azure) ❌                    │
│     ├─ Azure CLI (az login) ✅ SUCCESS                       │
│     └─ Returns Bearer token                                 │
│                      ↓                                       │
│  6. Chat Completions API Call                               │
│     POST https://...cognitiveservices.azure.com/            │
│         openai/deployments/gpt-5.2-chat/                    │
│         chat/completions?api-version=2025-01-01-preview     │
│     Authorization: Bearer {token}                           │
│                      ↓                                       │
│  7. Response: "4" ✅                                         │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│              Azure Production Flow                          │
│                                                              │
│  1. App deployed to Azure App Service                       │
│                      ↓                                       │
│  2. System-assigned Managed Identity enabled                │
│                      ↓                                       │
│  3. MANAGED_IDENTITY_CLIENT_ID not set (empty)              │
│                      ↓                                       │
│  4. DefaultAzureCredential tries:                           │
│     ├─ Environment variables ❌                              │
│     ├─ Managed Identity ✅ SUCCESS                           │
│     │  (Detected from Azure environment)                    │
│     └─ Returns Bearer token                                 │
│                      ↓                                       │
│  5. Same API call, same code ✅                             │
└────────────────────────────────────────────────────────────┘
```

---

## 🚀 Deployment Architecture

### Local Development

```
┌─────────────────────────────────────────────────────────┐
│                    Developer Machine                     │
│                                                           │
│  ┌────────────────────────────────────────────────────┐ │
│  │              PicoClaw Binary                        │ │
│  │  • Built from source: go build                      │ │
│  │  • Config: ~/.picoclaw/config.json                  │ │
│  │  • State: ~/.picoclaw/state.db                      │ │
│  │  • Skills: ~/.picoclaw/skills/                      │ │
│  └────────────────────────────────────────────────────┘ │
│                           ↓                              │
│  ┌────────────────────────────────────────────────────┐ │
│  │          Authentication Methods                     │ │
│  │  • Anthropic: macOS Keychain / API Key              │ │
│  │  • OpenAI: API Key / OAuth                          │ │
│  │  • Azure: Azure CLI (az login)                      │ │
│  └────────────────────────────────────────────────────┘ │
│                           ↓                              │
│  ┌────────────────────────────────────────────────────┐ │
│  │              External Services                      │ │
│  │  • LLM APIs (Claude, OpenAI, Azure)                 │ │
│  │  • Chat platforms (Discord, Telegram, etc.)         │ │
│  └────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### Docker Deployment

```
┌─────────────────────────────────────────────────────────┐
│                    Docker Host                           │
│                                                           │
│  ┌────────────────────────────────────────────────────┐ │
│  │           PicoClaw Container                        │ │
│  │  ┌──────────────────────────────────────────────┐  │ │
│  │  │  FROM golang:1.25-alpine                      │  │ │
│  │  │  WORKDIR /app                                 │  │ │
│  │  │  COPY . .                                     │  │ │
│  │  │  RUN go build -o picoclaw ./cmd/picoclaw/     │  │ │
│  │  │  CMD ["./picoclaw", "gateway"]                │  │ │
│  │  └──────────────────────────────────────────────┘  │ │
│  │                                                      │ │
│  │  Volumes:                                            │ │
│  │  • /app/data → ~/.picoclaw/ (state, skills)         │ │
│  │                                                      │ │
│  │  Environment:                                        │ │
│  │  • ANTHROPIC_API_KEY                                │ │
│  │  • AZURE_OPENAI_* variables                         │ │
│  │  • DISCORD_BOT_TOKEN                                │ │
│  │  • etc.                                              │ │
│  │                                                      │ │
│  │  Ports:                                              │ │
│  │  • 8080 (gateway HTTP)                              │ │
│  └────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### Azure Cloud Deployment

```
┌────────────────────────────────────────────────────────────────┐
│                         Azure Cloud                             │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Azure App Service                            │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │         PicoClaw Web App                           │  │  │
│  │  │  • Runtime: Go 1.25                                │  │  │
│  │  │  • Managed Identity: ✅ Enabled                    │  │  │
│  │  │  • App Settings:                                   │  │  │
│  │  │    - AZURE_OPENAI_ENDPOINT                         │  │  │
│  │  │    - AZURE_OPENAI_DEPLOYMENT                       │  │  │
│  │  │    - (No API keys needed!)                         │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                          ↓                                │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │      System-Assigned Managed Identity              │  │  │
│  │  │  • Principal ID: auto-generated                    │  │  │
│  │  │  • RBAC: "Cognitive Services OpenAI User"          │  │  │
│  │  └────────────────┬───────────────────────────────────┘  │  │
│  └───────────────────┼──────────────────────────────────────┘  │
│                      │                                          │
│  ┌───────────────────▼──────────────────────────────────────┐  │
│  │           Azure Active Directory                         │  │
│  │  • Validates managed identity                            │  │
│  │  • Issues bearer token                                   │  │
│  │  • Scope: https://cognitiveservices.azure.com/.default   │  │
│  └───────────────────┬──────────────────────────────────────┘  │
│                      │                                          │
│  ┌───────────────────▼──────────────────────────────────────┐  │
│  │         Azure OpenAI Service                             │  │
│  │  Resource: datacopilothub8882317788                      │  │
│  │  Deployment: gpt-5.2-chat                                │  │
│  │  Location: East US 2                                     │  │
│  │                                                            │  │
│  │  RBAC Permissions:                                        │  │
│  │  • Managed Identity → Cognitive Services OpenAI User     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Azure Storage (Optional)                     │  │
│  │  • State persistence                                      │  │
│  │  • Session history                                        │  │
│  │  • Skill storage                                          │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Azure Integration

### Azure OpenAI Architecture

```
┌───────────────────────────────────────────────────────────────┐
│                 PicoClaw Azure Integration                     │
│                                                                 │
│  Application Layer                                             │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  pkg/providers/codex_provider.go                         │  │
│  │  ┌───────────────────────────────────────────────────┐  │  │
│  │  │  NewCodexProviderAuto()                           │  │  │
│  │  │    ↓                                               │  │  │
│  │  │  LoadAzureConfigFromEnv()                         │  │  │
│  │  │    • AZURE_OPENAI_ENDPOINT                        │  │  │
│  │  │    • AZURE_OPENAI_DEPLOYMENT                      │  │  │
│  │  │    • AZURE_OPENAI_API_VERSION                     │  │  │
│  │  │    • AZURE_OPENAI_SCOPE                           │  │  │
│  │  │    • AZURE_OPENAI_MANAGED_IDENTITY_CLIENT_ID     │  │  │
│  │  │    ↓                                               │  │  │
│  │  │  createAzureManagedIdentityTokenSource()          │  │  │
│  │  │    ↓                                               │  │  │
│  │  │  if ManagedIdentityID != "":                      │  │  │
│  │  │    NewManagedIdentityCredential(clientID)         │  │  │
│  │  │  else:                                             │  │  │
│  │  │    NewDefaultAzureCredential()                    │  │  │
│  │  └───────────────────────────────────────────────────┘  │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  SDK Layer (Azure Identity)                                    │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  github.com/Azure/azure-sdk-for-go/sdk/azidentity      │  │
│  │                                                          │  │
│  │  DefaultAzureCredential Chain:                          │  │
│  │  1. EnvironmentCredential                              │  │
│  │     (AZURE_CLIENT_ID, AZURE_CLIENT_SECRET, etc.)       │  │
│  │  2. ManagedIdentityCredential                           │  │
│  │     (When running in Azure)                             │  │
│  │  3. AzureCLICredential                                  │  │
│  │     (az login - local development) ✅                    │  │
│  │  4. AzurePowerShellCredential                           │  │
│  │  5. InteractiveBrowserCredential                        │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  API Layer                                                     │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │  chatAzure() - Azure-specific implementation            │  │
│  │                                                          │  │
│  │  • Build chat messages                                  │  │
│  │  • Set max_completion_tokens (not max_tokens)          │  │
│  │  • Add api-version query parameter                     │  │
│  │  • Call Chat Completions API (not Responses API)       │  │
│  │  • Parse Azure response format                         │  │
│  └─────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────┘
```

### Environment Configuration Matrix

| Environment | Auth Method | Config Required | Works? |
|-------------|-------------|-----------------|--------|
| **Local Dev** (Azure CLI) | AzureCLICredential | `az login` + AZURE_OPENAI_* env vars | ✅ Yes |
| **Local Dev** (Service Principal) | EnvironmentCredential | AZURE_CLIENT_ID, AZURE_CLIENT_SECRET, AZURE_TENANT_ID | ✅ Yes |
| **Azure App Service** (System MI) | ManagedIdentityCredential | Managed Identity enabled | ✅ Yes |
| **Azure App Service** (User MI) | ManagedIdentityCredential | MANAGED_IDENTITY_CLIENT_ID set | ✅ Yes |
| **Azure VM** (System MI) | ManagedIdentityCredential | Managed Identity enabled | ✅ Yes |
| **Azure Container Instance** | ManagedIdentityCredential | Managed Identity assigned | ✅ Yes |
| **CI/CD** | EnvironmentCredential | Service Principal credentials | ✅ Yes |

---

## 💻 Technology Stack

### Core Technologies

| Component | Technology | Version | Purpose |
|-----------|------------|---------|---------|
| **Runtime** | Go | 1.25+ | Core language |
| **LLM SDKs** | anthropic-sdk-go | latest | Claude API |
| | openai-go | v3 | OpenAI & Azure OpenAI |
| **Azure SDKs** | azure-sdk-for-go/sdk/azidentity | v1.13+ | Azure authentication |
| | azure-sdk-for-go/sdk/azcore | v1.21+ | Azure core |
| **Database** | SQLite | 3 | Local state storage |
| **Messaging** | discordgo, telegram-bot-api | latest | Chat platforms |
| **Build** | Go modules | - | Dependency management |

### External Services

| Service | Purpose | Authentication |
|---------|---------|----------------|
| **Anthropic API** | Claude models | API Key, OAuth, Keychain |
| **OpenAI API** | GPT models | API Key, OAuth |
| **Azure OpenAI** | Azure-hosted models | Managed Identity, Azure CLI |
| **Discord** | Chat platform | Bot token |
| **Telegram** | Chat platform | Bot token |
| **Slack** | Enterprise chat | OAuth, Webhook |

---

## 🔒 Security Architecture

### Authentication Security

```
┌────────────────────────────────────────────────────────────┐
│              Security Layers                                │
│                                                              │
│  1. Credential Storage                                      │
│     ├─ macOS Keychain (encrypted)                          │
│     ├─ Environment variables (process-scoped)              │
│     ├─ Config files (file permissions)                     │
│     └─ Azure Managed Identity (no credentials stored)      │
│                                                              │
│  2. Transport Security                                      │
│     ├─ HTTPS/TLS for all API calls                         │
│     ├─ Bearer token authentication                          │
│     └─ Certificate validation                               │
│                                                              │
│  3. Access Control                                          │
│     ├─ RBAC for Azure resources                            │
│     ├─ API key scoping                                      │
│     └─ Rate limiting                                        │
│                                                              │
│  4. Runtime Security                                        │
│     ├─ Sandboxed tool execution                            │
│     ├─ Input validation                                     │
│     └─ Error sanitization                                   │
└────────────────────────────────────────────────────────────┘
```

### Azure Security Best Practices

✅ **Implemented**:
- Managed Identity (no credentials in code/config)
- RBAC for Azure OpenAI access
- DefaultAzureCredential for multi-environment support
- Bearer token authentication
- HTTPS-only API calls

🔒 **Recommended**:
- Azure Key Vault for secrets management
- Private endpoints for Azure OpenAI
- Network security groups
- Audit logging
- Cost management alerts

---

## 📈 Scalability Considerations

### Horizontal Scaling

```
┌─────────────────────────────────────────────────────────┐
│              Load Balancer                               │
│         (Azure Load Balancer / NGINX)                    │
└────────┬──────────────┬──────────────┬──────────────────┘
         │              │              │
    ┌────▼────┐    ┌────▼────┐    ┌────▼────┐
    │PicoClaw │    │PicoClaw │    │PicoClaw │
    │Instance1│    │Instance2│    │Instance3│
    └────┬────┘    └────┬────┘    └────┬────┘
         │              │              │
         └──────────────┴──────────────┘
                        │
         ┌──────────────▼──────────────┐
         │    Shared State Storage     │
         │  (Azure Storage / Redis)    │
         └─────────────────────────────┘
```

### Performance Optimization

- **Caching**: Session history, provider responses
- **Connection Pooling**: HTTP clients, database connections
- **Async Processing**: Tool execution, message handling
- **Resource Limits**: Max iterations, timeout controls

---

## 🎯 Summary

PicoClaw implements a **modular, scalable architecture** with:

✅ **Layered Design**: Client → Gateway → Agent → Services → Infrastructure
✅ **Provider Abstraction**: Support for multiple LLM providers
✅ **Extensibility**: Skills and tools plugin system
✅ **Multi-Channel**: Discord, Telegram, Slack, CLI, and more
✅ **Enterprise Auth**: Azure Managed Identity, OAuth, API Keys
✅ **Deployment Flexibility**: Local, Docker, Azure Cloud
✅ **Production Ready**: State management, logging, health monitoring

**Key Achievement**: Seamless Azure OpenAI integration with DefaultAzureCredential supporting both local development (Azure CLI) and production deployment (Managed Identity) with the same codebase.

---

**Document Version**: 1.0.0
**Last Updated**: February 15, 2026
**Status**: Production Ready

---

## 📊 Architecture Diagrams

High-resolution PNG diagrams are available in the [`docs/diagrams/`](docs/diagrams/) directory.

### Quick Links

- 🏗️ [High-Level Architecture](docs/diagrams/picoclaw-high-level.png) - System overview with all layers
- ☁️ [Azure Integration](docs/diagrams/picoclaw-azure-integration.png) - Azure OpenAI with Managed Identity
- 📊 [Data Flow](docs/diagrams/picoclaw-data-flow.png) - End-to-end message processing
- 🚀 [Deployment](docs/diagrams/picoclaw-deployment.png) - Local, Docker, and Azure deployment
- 🔌 [Providers](docs/diagrams/picoclaw-providers.png) - LLM provider architecture

**Full documentation**: [docs/ARCHITECTURE_DIAGRAMS.md](docs/ARCHITECTURE_DIAGRAMS.md)

**Specifications**:
- Format: PNG (300 DPI)
- Font: Arial Bold
- Total Size: ~4.2 MB
- Style: Professional, enterprise-ready

